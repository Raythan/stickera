name = "stickera-trade-registry"
main = "src/index.ts"
compatibility_date = "2024-11-01"

[[durable_objects.bindings]]
name = "REGISTRY"
class_name = "TradeRegistryDO"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["TradeRegistryDO"]

[vars]
# Comma-separated origins for CORS (override in dashboard if needed)
ALLOWED_ORIGINS = "https://raythan.github.io,http://localhost:8081,http://localhost:19006"
